export const DEFAULT_API_PREFIX = 'api';

export const DEFAULT_API_VERSION = '1';

export const DEFAULT_VERSION = '';

export const DEFAULT_PAGE = 1;

export const DEFAULT_LIMIT = 20;

export const USERNAME_CREDENTIAL = 'loginId';

export const PASSWORD_CREDENTIAL = 'password';

export const DEFAULT_APP_URL = 'https://example.com';

export const DEFAULT_LANGUAGE = 'jp';

export const SEEDING_START_LINE = 7;

export const BATCH_HOST = '0.0.0.0';

export const BATCH_PORT = +(process.env['BATCH_PORT'] ?? 3001);

export const IMAGE_MAX_SIZE = 1024 * 5 * 1000; // 5MB
